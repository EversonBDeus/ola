export interface BasDados {
    baCentral: string;
    baStatus: string;
    date: string;
    baNumber: number;
    baUF: string;
    id?: string;
    hidden?: boolean; // Adicionando a propriedade 'hidden'
  }